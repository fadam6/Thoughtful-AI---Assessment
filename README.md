# Repo for Thoughtful AI's technical assessment for FDE position

This repo contains a Python implementation of `sort(width, height, length, mass)` to route packages into stacks based on **bulky** and **heavy** rules.

## Files
- `main.py`: implementation of the sort() function
- `test_main.py`: pytest test suite

## Run Locally
### Run the function manually
```bash
python main.py

